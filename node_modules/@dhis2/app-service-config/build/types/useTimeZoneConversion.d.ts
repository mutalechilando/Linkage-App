import { DateInput } from './types';
declare class DHIS2Date extends Date {
    serverOffset: number;
    serverTimezone: string;
    clientTimezone: string;
    constructor({ date, serverOffset, serverTimezone, clientTimezone, }: {
        date: DateInput;
        serverOffset: number;
        serverTimezone: string;
        clientTimezone: string;
    });
    private _getISOString;
    getServerZonedISOString(): string;
    getClientZonedISOString(): string;
}
export declare const useTimeZoneConversion: () => {
    fromServerDate: (date?: DateInput | undefined) => DHIS2Date;
    fromClientDate: (date?: DateInput | undefined) => DHIS2Date;
};
export {};
