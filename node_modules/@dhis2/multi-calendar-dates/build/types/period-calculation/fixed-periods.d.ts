import { SupportedCalendar } from '../types';
declare const periodIdentifiers: readonly ["DAILY", "WEEKLY", "WEEKLYWED", "WEEKLYTHU", "WEEKLYSAT", "WEEKLYSUN", "BIWEEKLY", "MONTHLY", "BIMONTHLY", "QUARTERLY", "QUARTERLYNOV", "SIXMONTHLY", "SIXMONTHLYAPR", "SIXMONTHLYNOV", "YEARLY", "FYNOV", "FYOCT", "FYJUL", "FYAPR"];
export declare type PeriodIdentifier = typeof periodIdentifiers[number];
export declare type FixedPeriod = {
    id: string;
    iso?: string;
    name: string;
    startDate: string;
    endDate: string;
};
declare type GeneratedPeriodParams = {
    year: number;
    periodType: PeriodIdentifier;
    calendar: SupportedCalendar;
    locale?: string;
    startingDay?: number; /** 1 is Monday */
    yearsCount?: number;
};
export declare type GeneratedPeriodsFunc = (options: GeneratedPeriodParams) => Array<FixedPeriod>;
declare const generateFixedPeriods: GeneratedPeriodsFunc;
export default generateFixedPeriods;
